version: '3.8'

services:
  rbrcs:
    build: .
    container_name: rbrcs
    ports:
      - "5000:5000"
      - "1514:514/udp"
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped
