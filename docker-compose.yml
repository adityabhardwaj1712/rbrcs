version: '3.8'

services:
  rbrcs:
    build: .
    container_name: rbrcs
    ports:
      - "8080:8080"
      - "1514:514/udp"
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
